 <div class="container">
    <nav class="navbar navbar-expand-lg navbar-light bg-primary">
        <%= link_to "Главная", root_path, class:'navbar-brand' %> 
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <%= link_to "О нас", pages_about_path, class:'nav-item nav-link' %>
                    <% if !user_signed_in? %>
                       <%= link_to "Создать аккаунт", new_user_registration_path, class:'nav-item nav-link' %>
                       <%= link_to "Войти", new_user_session_path, class:'nav-item nav-link' %>
                    <% end %>
                    <% if user_signed_in? %>
                        <%= link_to "Выйти", destroy_user_session_path, method: :delete, class:'nav-item nav-link' %>
                        <%= link_to "Объекты", objs_path, class:'nav-item nav-link' %>
                    <% end %>
            </div>
        </div>
    </nav>